# Backend API URL for deployed environments (e.g. https://petrolq-api.snapdeploy.app)
# Leave unset for local development — defaults to http://localhost:8000
VITE_API_URL=

# Enable this to use locally downloaded raster map tiles instead of online CartoDB vector tiles
VITE_OFFLINE_MODE=false
